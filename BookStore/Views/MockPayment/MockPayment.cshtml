@model BookStore.Models.Order
@{
    ViewData["Title"] = "Mock Payment Gateway";
    var order = ViewBag.Order as BookStore.Models.Order;
    var orderId = ViewBag.OrderId as int? ?? 0;
    var amount = ViewBag.Amount as decimal? ?? 0;
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0">
                <div class="card-header bg-primary text-white text-center py-4">
                    <h3 class="mb-0">
                        <i class="bi bi-credit-card-2-front me-2"></i>
                        Mock Payment Gateway
                    </h3>
                    <p class="mb-0 mt-2 small opacity-75">Trang thanh toán giả lập cho mục đích test</p>
                </div>
                <div class="card-body p-4">
                    <div class="alert alert-info mb-4">
                        <i class="bi bi-info-circle me-2"></i>
                        <strong>Test Cards:</strong> Dùng số thẻ bắt đầu bằng <strong>4</strong> hoặc <strong>5</strong> để test thành công, 
                        số thẻ bắt đầu bằng <strong>9</strong> để test thất bại. Hoặc nhập bất kỳ số thẻ nào để test.
                    </div>

                    <div class="mb-4">
                        <h5 class="text-muted mb-3">
                            <i class="bi bi-receipt me-2"></i>Thông tin đơn hàng
                        </h5>
                        <div class="table-responsive">
                            <table class="table table-bordered">
                                <tr>
                                    <td class="fw-semibold">Mã đơn hàng:</td>
                                    <td>#@orderId</td>
                                </tr>
                                <tr>
                                    <td class="fw-semibold">Tổng tiền:</td>
                                    <td class="text-success fw-bold fs-5">@amount.ToString("N0") ₫</td>
                                </tr>
                                <tr>
                                    <td class="fw-semibold">Trạng thái:</td>
                                    <td>
                                        <span class="badge bg-warning">Chờ thanh toán</span>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>

                    <hr class="my-4" />

                    <form method="post" action="@Url.Action("ProcessPayment", "MockPayment")" id="paymentForm">
                        <input type="hidden" name="orderId" value="@orderId" />
                        
                        <h5 class="text-muted mb-3">
                            <i class="bi bi-credit-card me-2"></i>Thông tin thẻ thanh toán
                        </h5>

                        <div class="mb-3">
                            <label for="cardNumber" class="form-label fw-semibold">
                                Số thẻ <span class="text-danger">*</span>
                            </label>
                            <div class="input-group">
                                <input type="text" 
                                       class="form-control form-control-lg" 
                                       id="cardNumber" 
                                       name="cardNumber" 
                                       placeholder="1234 5678 9012 3456"
                                       maxlength="19"
                                       required
                                       autocomplete="cc-number" />
                                <span class="input-group-text" id="cardType">
                                    <i class="bi bi-credit-card"></i>
                                </span>
                            </div>
                            <div class="form-text">
                                <i class="bi bi-info-circle me-1"></i>
                                Ví dụ: <strong>4111 1111 1111 1111</strong> (Visa - thành công) hoặc 
                                <strong>5555 5555 5555 4444</strong> (Mastercard - thành công) hoặc 
                                <strong>9000 0000 0000 0000</strong> (thất bại)
                            </div>
                            <div class="invalid-feedback">Vui lòng nhập số thẻ hợp lệ</div>
                        </div>

                        <div class="mb-3">
                            <label for="cardHolderName" class="form-label fw-semibold">
                                Tên chủ thẻ <span class="text-danger">*</span>
                            </label>
                            <input type="text" 
                                   class="form-control form-control-lg" 
                                   id="cardHolderName" 
                                   name="cardHolderName" 
                                   placeholder="NGUYEN VAN A"
                                   required
                                   autocomplete="cc-name" />
                            <div class="invalid-feedback">Vui lòng nhập tên chủ thẻ</div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="expiryDate" class="form-label fw-semibold">
                                    Ngày hết hạn <span class="text-danger">*</span>
                                </label>
                                    <input type="text" 
                                           class="form-control form-control-lg" 
                                           id="expiryDate" 
                                           name="expiryDate" 
                                           placeholder="MM/YY"
                                           maxlength="5"
                                           required
                                           autocomplete="cc-exp" />
                                <div class="form-text">Ví dụ: 12/25</div>
                                <div class="invalid-feedback">Vui lòng nhập ngày hết hạn (MM/YY)</div>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="cvv" class="form-label fw-semibold">
                                    CVV <span class="text-danger">*</span>
                                </label>
                                <input type="text" 
                                       class="form-control form-control-lg" 
                                       id="cvv" 
                                       name="cvv" 
                                       placeholder="123"
                                       maxlength="4"
                                       required
                                       autocomplete="cc-csc" />
                                <div class="form-text">3 hoặc 4 chữ số</div>
                                <div class="invalid-feedback">Vui lòng nhập CVV</div>
                            </div>
                        </div>

                        <div class="d-grid gap-2 mt-4">
                            <button type="submit" class="btn btn-primary btn-lg" id="submitBtn">
                                <i class="bi bi-lock-fill me-2"></i>
                                Thanh toán @amount.ToString("N0") ₫
                            </button>
                            <a href="@Url.Action("Index", "Cart")" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left me-2"></i>
                                Hủy và quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card mt-3 border-info">
                <div class="card-body bg-light">
                    <small class="text-muted">
                        <i class="bi bi-info-circle me-1 text-info"></i>
                        <strong>Website Test:</strong> Đây là website dùng để test và demo. 
                        Mock Payment Gateway cho phép bạn test quy trình đặt hàng hoàn chỉnh mà không cần thanh toán thật.
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border-radius: 15px;
        overflow: hidden;
    }
    
    .card-header {
        border-radius: 0;
    }
    
    .table td {
        padding: 0.75rem;
    }
    
    .form-control-lg {
        padding: 0.75rem 1rem;
        font-size: 1rem;
    }

    #cardNumber {
        font-family: 'Courier New', monospace;
        letter-spacing: 2px;
        font-size: 1.1rem;
    }

    #cardHolderName {
        text-transform: uppercase;
    }

    .btn-lg {
        padding: 0.75rem 1.5rem;
        font-size: 1.1rem;
    }
</style>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const cardNumberInput = document.getElementById('cardNumber');
            const expiryDateInput = document.getElementById('expiryDate');
            const cvvInput = document.getElementById('cvv');
            const paymentForm = document.getElementById('paymentForm');
            const submitBtn = document.getElementById('submitBtn');

            // Format số thẻ: tự động thêm khoảng trắng mỗi 4 số và hiển thị loại thẻ
            cardNumberInput.addEventListener('input', function(e) {
                let value = e.target.value.replace(/\s/g, ''); // Xóa tất cả khoảng trắng
                let formattedValue = value.match(/.{1,4}/g)?.join(' ') || value; // Thêm khoảng trắng mỗi 4 số
                e.target.value = formattedValue;
                
                // Hiển thị loại thẻ
                const cardTypeIcon = document.getElementById('cardType');
                const firstDigit = value.charAt(0);
                if (firstDigit === '4') {
                    cardTypeIcon.innerHTML = '<i class="bi bi-credit-card text-primary"></i> <small class="text-muted">Visa</small>';
                } else if (firstDigit === '5') {
                    cardTypeIcon.innerHTML = '<i class="bi bi-credit-card text-danger"></i> <small class="text-muted">Mastercard</small>';
                } else if (firstDigit === '9') {
                    cardTypeIcon.innerHTML = '<i class="bi bi-credit-card text-warning"></i> <small class="text-muted">Test Fail</small>';
                } else if (value.length > 0) {
                    cardTypeIcon.innerHTML = '<i class="bi bi-credit-card"></i>';
                } else {
                    cardTypeIcon.innerHTML = '<i class="bi bi-credit-card"></i>';
                }
            });

            // Format ngày hết hạn: MM/YY
            expiryDateInput.addEventListener('input', function(e) {
                let value = e.target.value.replace(/\D/g, ''); // Chỉ giữ số
                if (value.length >= 2) {
                    value = value.substring(0, 2) + '/' + value.substring(2, 4);
                }
                e.target.value = value;
            });

            // Chỉ cho phép nhập số cho CVV
            cvvInput.addEventListener('input', function(e) {
                e.target.value = e.target.value.replace(/\D/g, '');
            });

            // Validation và submit
            paymentForm.addEventListener('submit', function(e) {
                e.preventDefault();
                
                // Kiểm tra validation
                if (!paymentForm.checkValidity()) {
                    paymentForm.classList.add('was-validated');
                    return;
                }

                // Disable button và hiển thị loading
                submitBtn.disabled = true;
                const originalText = submitBtn.innerHTML;
                submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Đang xử lý...';

                // Lấy số thẻ (bỏ khoảng trắng)
                const cardNumber = cardNumberInput.value.replace(/\s/g, '');
                const firstDigit = cardNumber.charAt(0);

                // Xác định kết quả dựa trên số thẻ
                let result = 'success';
                if (firstDigit === '9') {
                    result = 'failed';
                } else if (firstDigit === '4' || firstDigit === '5') {
                    result = 'success';
                } else {
                    // Mặc định thành công nếu form hợp lệ
                    result = 'success';
                }

                // Tạo form ẩn để submit
                const hiddenForm = document.createElement('form');
                hiddenForm.method = 'POST';
                hiddenForm.action = '@Url.Action("ProcessPayment", "MockPayment")';
                
                const orderIdInput = document.createElement('input');
                orderIdInput.type = 'hidden';
                orderIdInput.name = 'orderId';
                orderIdInput.value = '@orderId';
                
                const resultInput = document.createElement('input');
                resultInput.type = 'hidden';
                resultInput.name = 'result';
                resultInput.value = result;

                hiddenForm.appendChild(orderIdInput);
                hiddenForm.appendChild(resultInput);
                document.body.appendChild(hiddenForm);
                hiddenForm.submit();
            });
        });
    </script>
}
